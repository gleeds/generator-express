{
  "name": "<%= _.slugify(appname) %>",
  "version": "0.0.1",
  "ignore": [
    "**/.*",
    "node_modules",
    "components"
  ]<% if(options.bootstrap) { %> ,
  "dependencies": {
    "bootstrap": "~3.2.0"
  }
  <% } %>
}
